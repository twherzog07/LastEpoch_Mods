using HarmonyLib;
using Il2Cpp;

namespace LastEpoch_Hud.Scripts.Mods.Factions
{
    public class Factions_SoulOfErasedLoot
    {
        /*[HarmonyPatch(typeof(SilkenCocoonData), "DropSoulOftheErasedLoot")]
        public class SilkenCocoonData_DropSoulOftheErasedLoot
        {
            [HarmonyPrefix]
            static void Prefix(UnityEngine.Vector3 __0, int __1, int __2, float __3, int __4, int __5, int __6, Il2Cpp.ItemCreationProperties.PlayerGroup __7)
            {
                Main.logger_instance.Msg("SilkenCocoonData.DropSoulOftheErasedLoot(position, level, corruption, increasedItemRarity, dungeonOrArenaTier, islandTier, maxIslandTierForNonShadeEchoes, playerGroup) Prefix");
                //Main.logger_instance.Msg("position = " + __0.ToString());
                //Main.logger_instance.Msg("level = " + __1.ToString());
                //Main.logger_instance.Msg("corruption = " + __2.ToString());
                Main.logger_instance.Msg("increasedItemRarity = " + __3.ToString());
                //Main.logger_instance.Msg("dungeonOrArenaTier = " + __4.ToString());
                //Main.logger_instance.Msg("islandTier = " + __5.ToString());
                //Main.logger_instance.Msg("maxIslandTierForNonShadeEchoes = " + __6.ToString());
                //Main.logger_instance.Msg("playerGroup = " + __7.ToString());
            }
        }*/
    }
}
